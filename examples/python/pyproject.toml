[project]
name = "toolpath-example-python"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["httpx-sse>=0.4.0,<0.5.0", "toolpath"]

[tool.uv.sources]
toolpath = { path = "../../packages/sdk-python" }

[tool.ruff]
line-length = 88
