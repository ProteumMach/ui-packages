name: Release

on:
  workflow_dispatch:
    inputs:
      package:
        description: Package family to build
        required: true
        type: choice
        options:
          - sdk
          - ui
          - viewer
      publish:
        description: Publish the reviewed tag to the registries
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24.18.0
          package-manager-cache: false
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        if: inputs.package != 'sdk'
        with:
          version: 0.11.28
          python-version: '3.11'
      - run: pnpm install --frozen-lockfile
      - name: Validate the selected package
        run: |
          if [ "${{ inputs.package }}" = "sdk" ]; then
            pnpm release:typescript-sdk:check
          else
            pnpm check
          fi
      - run: node scripts/prepare-release.mjs
        env:
          RELEASE_PACKAGE: ${{ inputs.package }}
          PUBLISH: ${{ inputs.publish }}
      - uses: actions/upload-artifact@v7
        with:
          name: release-packages
          path: |
            release-artifacts/*
          if-no-files-found: error
          retention-days: 3

  publish-npm:
    if: inputs.publish
    needs: build-and-test
    runs-on: ubuntu-latest
    environment: publish
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v7
        with:
          name: release-packages
          path: release-artifacts
      - uses: actions/setup-node@v6
        with:
          node-version: 24.18.0
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: npm install --global npm@11.19.0
      - name: Publish TypeScript SDK
        if: inputs.package == 'sdk'
        run: npm publish release-artifacts/toolpath-api-*.tgz --access public --provenance
      - name: Publish UI package
        if: inputs.package == 'ui'
        run: npm publish release-artifacts/toolpath-ui-*.tgz --access public
      - name: Publish viewer package
        if: inputs.package == 'viewer'
        run: npm publish release-artifacts/toolpath-viewer-*.tgz --access public
