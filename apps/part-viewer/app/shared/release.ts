/**
 * What this release shows.
 *
 * Directions are built and tested — the tab, the arrows on the part, the
 * per-direction counts, the colour a direction wears — but they are not in the
 * first release, and a half-told story is worse than one not started: a
 * Directions tab that shows candidate directions the plan does not yet use
 * invites somebody to act on a choice the app cannot keep.
 *
 * So they are hidden here rather than deleted. Everything behind this flag
 * still compiles, still has its tests, and comes back by making this `true`.
 */
export const SHOWS_DIRECTIONS = false
